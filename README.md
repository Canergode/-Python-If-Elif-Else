## What I Learned

- How to use `if`, `elif`, and `else` to create branching logic
- How to compare a variable against a value using comparison operators (`>`, `<`)
- How Python evaluates conditions in order and stops at the first match
- How to use `print()` to display different messages based on a condition
